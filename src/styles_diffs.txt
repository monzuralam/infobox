1) boxWrapperStyle -> save.js -> 
    cursor: isClickable ? "pointer" : "default",
2) imageWrapperStyle -> edit.js ->
    display: imageOrIcon === "image" ? "block" : "none",
    * diff:
    edit.js : 
        backgroundImage:
        imageOrIcon === "image" && imageUrl ? `url(${imageUrl})` : "none",
    save.js : backgroundImage: imageUrl ? `url(${imageUrl})` : undefined,
3) contentStyle -> 
    *diff: 
    edit.js : 
        color: contentColor || "#5b5b5b",
    save.js : 
        color: contentColor,